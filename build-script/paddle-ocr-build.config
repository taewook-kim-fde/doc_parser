# 빌드 컨텍스트: 프로젝트 루트
CONTEXT=.

# Dockerfile 위치
DOCKERFILE=genon/serving/paddle/docker/Dockerfile

# 이미지 이름/태그
IMAGE_NAME=doc-parser-ocr
IMAGE_TAG="v2.2.1-ssr-$(git rev-parse --short=4 HEAD)-cve"

# 푸시할 레지스트리 (없으면 빈값)
REGISTRY=genonai

# Paddle extra index (없으면 pyproject의 값 사용)
PADDLE_EXTRA_INDEX_URL=https://www.paddlepaddle.org.cn/packages/stable/cu126/